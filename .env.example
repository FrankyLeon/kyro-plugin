# BEP-20 mode is set in wp-config.php, not this file:
#
#   define( 'SCP_BEP20_MODE', 'test' );   // fake USDT, no real money
#   define( 'SCP_BEP20_MODE', 'live' );   // real BSC USDT
#
# Put it above: /* That's all, stop editing! Happy publishing. */
